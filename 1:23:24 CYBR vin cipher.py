




keyword = "LEMON"

message = "I want to encrypt this message"

len_of_message = len(message)
math.ceil(len_of_message/ len(keyword))

for char in message:
    if char.isalpha():
        is char.isupper():


            char_ord = ord(char) - 65

            key_val = ord(key_char) - 65

            encr_val
